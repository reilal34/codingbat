public int makeChocolate(int small, int big, int goal) {
  

   int smallNeeded = goal - big * 5;
    


  if(big*5>goal){
    smallNeeded=goal%5;
  }

      if (small + big*5 < goal || small< smallNeeded)  return -1;

    return smallNeeded;
}
